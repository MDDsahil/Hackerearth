#include <stdio.h>
#include <string.h>

int main(){
    
    char s[100],r[100];
    int begin, end;
    gets(s);
    
    end=strlen(s)-1;
    
    for(begin=0;begin<strlen(s);begin++){
        r[begin]=s[end];
        end--;
    }
    r[begin]='\0';
    printf("%s\n",r);
    return 0;
}
